import { TwoDigitInputDirective } from './two-digit-input.directive';

describe('TwoDigitInputDirective', () => {
  it('should create an instance', () => {
    const directive = new TwoDigitInputDirective();
    expect(directive).toBeTruthy();
  });
});
